version: "3.9"

services:
  frontend:
    image: your-dockerhub-username/llm-frontend:latest
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "8501:8501"
    depends_on:
      - api
    env_file:
      - .env

  api:
    image: your-dockerhub-username/llm-api:latest
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "5001:5001"
